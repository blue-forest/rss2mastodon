name: Check

on:
  workflow_dispatch:
  push:
    branches: ["master"]
  schedule:
  - cron: "*/30 * * * *"

permissions:
  contents: read

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
    - name: Repository
      uses: actions/checkout@v3
    - name: Setup Deno
      uses: denoland/setup-deno@v1
    - name: Run Deno
      env:
        TOKEN_01NET_RSS_TOOT_AQUILENET_FR: ${{ secrets.TOKEN_01NET_RSS_TOOT_AQUILENET_FR }}
      run: deno run --allow-all ./sync.ts
